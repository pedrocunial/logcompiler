{
    /* isso e um comentario e
       deveria
       ser
       ignorado
       rsrsrs
    */
    x = 3;
    y = 2;
    if (x > y || x == 2) {
        printf(x);
    };
    else {
        printf(y);
    };

    if (x == y)
        printf(x);
    else
        printf(y);

    while (scanf() < 3) {
        printf(x - 1);
        x = x - 1;
    };
    printf(x + y);
    printf(x - -y);
    printf(    /* bla */ 1 /* bla */);
    printf(  3+4/2 + y);
    z = x + y;
    printf(x + y + z);
    x = scanf() + 3;
    y = -scanf();
    printf(x);
    printf(y);
    printf(scanf());
}
